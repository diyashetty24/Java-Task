package student;
import java.io.BufferedWriter;
import java.io.FileWriter;
import java.io.IOException;

public class Score {
	 private final String filePath;
	 public Score(String filePath) {
	     this.filePath = filePath;
	 }
	 public synchronized void writeScore(String studentName, int roll, int marks) {
	     try (BufferedWriter writer = new BufferedWriter(new FileWriter(filePath, true))) {
	         writer.write(studentName + "," + roll + "," + marks);
	         writer.newLine();
	         System.out.println(Thread.currentThread().getName() + " logged score  " + studentName);
	     } catch (IOException e) {
	         System.err.println("Error writing to : " + e.getMessage());
	     }
	   }
	}

