package student;

public class MainClass {
	    public static void main(String[] args) {
	        System.out.println("=== Student Exam Score Logger ===\n");
	        String filePath = "scores.csv";
	        Score logger = new Score(filePath);

	        Thread student1 = new StudentLogger(logger, "Diya", 1 , 95);
	        Thread student2 = new StudentLogger(logger, "Aarav", 2 , 88);
	        Thread student3 = new StudentLogger(logger, "Meera", 3 , 92);

	        // Start all threads
	        student1.start();
	        student2.start();
	        student3.start();

	        // Wait for all threads to complete
	        try {
	            student1.join();
	            student2.join();
	            student3.join();
	        } catch (InterruptedException e) {
	            e.printStackTrace();
	        }

	        System.out.println("\nAll student scores logged successfully!!");
	        System.out.println("Check 'scores.csv' for output.\n");
	    }
	}


